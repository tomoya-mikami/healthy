import React, { FC } from "react";
import { Text, View } from "react-native";

export const Fugafuga: FC = (props: any) => {
  return (
    <View
      style={{
        flex: 1,
      }}
    >
      <Text>fugafuga</Text>
    </View>
  );
};
